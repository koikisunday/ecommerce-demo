# Use official Node image for devcontainer
FROM mcr.microsoft.com/devcontainers/javascript-node:0-20

# Install docker client tools
RUN apt-get update && apt-get install -y docker-compose-plugin docker.io || true
